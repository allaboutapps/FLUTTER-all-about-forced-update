name: all_about_forced_update
description: Force update an outdated app
version: 2.0.0
homepage: https://allaboutapps.at

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^5.0.1
  http: ^1.2.0
  open_store: ^0.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
